;(() => {
  const style = document.createElement("style")
  style.dataset.filerouterDocsBrand = ""
  style.textContent = ".nav-logo { height: 1.75rem !important; }"
  document.head.append(style)
})()
